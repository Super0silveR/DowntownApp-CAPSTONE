namespace Domain.Enums
{
    /// <summary>
    /// Enum that represents the classification for the tickets that are sold/reserved. 
    /// </summary>
    public enum TicketClassification
    {
        Default = 0,
        Vip = 1,
        Subscription = 2
    }
}
