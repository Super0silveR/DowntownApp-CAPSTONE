import { Theme, ThemeOptions } from "@mui/material/styles";

declare module '@mui/material/styles' {
    interface DowntownTheme extends Theme {
       
    }

    interface DowntownThemeOptions extends ThemeOptions {
        
    }
}